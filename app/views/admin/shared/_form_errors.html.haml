- if object.errors.any?
  .alert.alert-error
    %button.close{data:{dismiss: 'alert'}} x
    %p
      %strong= "Please correct the following #{pluralize(object.errors.count, 'error')}:"
    %ul
      - object.errors.full_messages.each do |msg|
        %li= msg