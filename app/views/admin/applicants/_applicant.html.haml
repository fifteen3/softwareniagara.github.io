%tr
  %td= applicant.name
  %td= applicant.email
  %td= applicant.created_at.strftime('%m/%d/%Y')
  %td= applicant.updated_at.strftime('%m/%d/%Y')
  %td
    %a{href: edit_admin_applicant_path(applicant), class: 'btn'}
      %i.icon-pencil
      Edit
    %a{href: admin_applicant_path(applicant), 'data-method' => :delete, 'data-confirm' => "Are you sure you want to remote '#{applicant.email}'?", class: 'btn'}
      %i.icon-remove
      Delete